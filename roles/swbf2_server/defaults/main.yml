---
# defaults file for swbf2_server
# Default variables for Star Wars Battlefront 2 game server deployment

# Windows Server compatibility
supported_windows_versions:
  - "Windows Server 2019"
  - "Windows Server 2022"
  - "Windows Server 2025"

# Ansible user configuration
# Default to 'ansible' if ansible_user is not set in inventory
ansible_user_name: "ansible"

virtio_iso_url: "{{ _virtio_iso_url }}"
virtio_update: false

# SWBF2 server zip URL
swbf2_server_zip_url: ""

# SWBF2 MySQL configuration
swbf2_mysql_database_name: ""
swbf2_mysql_username: ""
swbf2_mysql_password: ""